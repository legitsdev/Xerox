#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
VENV_PYTHON="$ROOT_DIR/.venv/bin/python"

if [[ ! -x "$VENV_PYTHON" ]]; then
  echo "Xerox is not installed yet. Run ./install.sh first." >&2
  exit 1
fi

cd "$ROOT_DIR"
exec "$VENV_PYTHON" -m xerox "$@"
